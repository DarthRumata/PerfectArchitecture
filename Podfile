# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PerfectArhitecture' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestDI
  pod 'NeedleFoundation'
  pod 'XCoordinator'
  pod 'Alamofire'
  
  # Modules
  pod 'ProductPage', :path => 'Features/ProductPage'
  pod 'Cart', :path => 'Features/Cart'
  
  # Core
  pod 'CommonLogic', :path => 'Core/CommonLogic'
  pod 'CommonUI', :path => 'Core/CommonUI'
  pod 'WLNetworkClient', :path => 'Core/NetworkClient'

end
